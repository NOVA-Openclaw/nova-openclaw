## Unreleased

### NOVA Fork

- Rebuilt the fork on upstream `v2026.9.6` and re-applied the three surviving fork patches that upstream does not carry: the `extensions/agent-identity-env/` plugin (`OPENCLAW_AGENT_ID`/`CLAWDBOT_AGENT_ID`/`OPENCLAW_SESSION_KEY` injection via the `resolve_exec_env` hook, replacing the old core-file patch whose target `src/agents/bash-tools.exec.ts` no longer exists), provider-refusal-triggers-model-fallback classification (`refusal` failover reason across `src/agents/failover/*`, `packages/ai/src/providers/*`, and `packages/gateway-protocol/src/failover-reasons.ts`), and synthetic `db:`/`fallback:` bootstrap path identifier preservation in `sanitizeBootstrapFiles` (`src/agents/bootstrap-files.ts`). Also re-applied the xurl `SKILL.md` media-upload-defaults warning. The bundled `himalaya` skill is intentionally kept in the repo per #212 scope; NOVA's running install removes it separately as post-deploy cleanup. Full patch audit, classification rationale, and staging/QA validation: (#212).
